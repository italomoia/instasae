# instasae — Environment Variables
# Copy to .env and fill in real values. NEVER commit .env to git.

# ── Server ──────────────────────────────────────────────
PORT=8080
LOG_LEVEL=debug   # debug | info | warn | error

# ── PostgreSQL ──────────────────────────────────────────
# Dev: postgres://instasae:instasae_dev@localhost:5433/instasae?sslmode=disable
# Prod: postgres://instasae:STRONG_PASSWORD@postgres:5432/instasae?sslmode=disable
DATABASE_URL=postgres://instasae:instasae_dev@localhost:5433/instasae?sslmode=disable

# ── Redis ───────────────────────────────────────────────
# Dev: redis://localhost:6380/0
# Prod: redis://redis:6379/2 (index 2 to avoid Chatwoot collision)
REDIS_URL=redis://localhost:6380/0

# ── Meta (Instagram) ───────────────────────────────────
# App Secret from Meta Developer Dashboard → App Settings → Basic
META_APP_SECRET=YOUR_META_APP_SECRET
# API version pinned
META_GRAPH_API_VERSION=v25.0

# ── Backblaze B2 (S3-compatible) ───────────────────────
B2_ENDPOINT=https://s3.us-west-004.backblazeb2.com
B2_REGION=us-west-004
B2_BUCKET=YOUR_BUCKET_NAME
B2_KEY_ID=YOUR_B2_KEY_ID
B2_APPLICATION_KEY=YOUR_B2_APP_KEY
B2_PUBLIC_URL=https://f004.backblazeb2.com/file/YOUR_BUCKET_NAME
B2_PREFIX=instasae

# ── Encryption ──────────────────────────────────────────
# 32 bytes as 64 hex characters. Generate with: openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ── Admin API ───────────────────────────────────────────
# Strong random string for X-API-Key header. Generate with: openssl rand -hex 24
ADMIN_API_KEY=YOUR_ADMIN_API_KEY

# ── Webhook ─────────────────────────────────────────────
# Global fallback verify token for Meta webhook handshake
WEBHOOK_VERIFY_TOKEN=YOUR_VERIFY_TOKEN
